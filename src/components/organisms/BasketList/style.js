import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  container: {
    marginTop: 50,
  },
  basket: {
    marginTop: 40,
  },
  title: {
    color: '#fff',
    fontSize: 27,
  },
  spinner: {
    paddingTop: 110,
  },
}));

export default useStyles;
