import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  wrapper: {
    flexGrow: 1,
  },
  warning: {
    display: 'block',
    margin: '0 auto',
    color: '#fff',
    fontSize: 20,
  },
}));

export default useStyles;
