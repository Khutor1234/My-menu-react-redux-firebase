export const isRequestSelector = (state) => state.menuReducer?.isRequest;
export const errorsSelector = (state) => state.menuReducer?.errors;
export const menuSelector = (state) => state.menuReducer?.menu;
